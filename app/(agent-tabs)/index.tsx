// Unified home: agents see the same marketplace/home feed as buyers.
// The agent's operational dashboard now lives behind the Profile tab's
// "Profile" toggle (see components/agent/AgentDashboard + (agent-tabs)/profile).
export { default } from "../(tabs)/index";
