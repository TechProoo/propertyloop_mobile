import { TabPlaceholder } from "@/components/brand/TabPlaceholder";

export default function InboxScreen() {
  return (
    <TabPlaceholder
      icon="chatbubble"
      eyebrow="INBOX"
      title="Talk to"
      italic="verified agents."
      body="Once you reach out to a listing agent or hire a vendor, every conversation lands here."
    />
  );
}
