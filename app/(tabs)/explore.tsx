import { TabPlaceholder } from "@/components/brand/TabPlaceholder";

export default function ExploreScreen() {
  return (
    <TabPlaceholder
      icon="search"
      eyebrow="EXPLORE"
      title="Map view + filters,"
      italic="coming next."
      body="Browse Lagos, Abuja and Port Harcourt on a map, with the same filters as the home feed."
    />
  );
}
