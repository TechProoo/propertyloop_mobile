// Unified home: vendors see the same marketplace/home feed as buyers.
// The vendor's operational dashboard now lives behind the Profile tab's
// "Profile" toggle (see components/vendor/VendorDashboard + (vendor-tabs)/profile).
export { default } from "../(tabs)/index";
